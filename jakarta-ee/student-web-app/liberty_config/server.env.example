# Example server.env configuration
# Copy this file to server.env and update the paths for your environment

SESSION_COOKIE_NAME=test-cookie-name
CLONE_ID=test-clone-id
SESSION_TIMEOUT=20m

# Set these environment variables based on your Open Liberty installation
# WLP_USER_DIR should point to your Open Liberty user directory
# SERVER_NAME should be the name of your server instance
# Example for Windows: WLP_USER_DIR=D:\open-liberty\openliberty-25.0.0.4\wlp\usr
# Example for Linux: WLP_USER_DIR=/opt/openliberty/wlp/usr
# SERVER_NAME=jimmyserver

# Liberty server configuration paths - these will be auto-constructed from above variables
LIBERTY_SERVER_DIR=${WLP_USER_DIR}/servers/${SERVER_NAME}
KEYSTORE_LOCATION=${LIBERTY_SERVER_DIR}/conf/server.p12
KEYSTORE_PASSWORD=admin123
TRUSTED_KEYSTORE_LOCATION=${LIBERTY_SERVER_DIR}/conf/truststore.p12
TRUSTED_KEYSTORE_PASSWORD=admin

LTPA_KEY_FILE_SFA=${LIBERTY_SERVER_DIR}/conf/ltpa_sfa.key
LTPA_KEY_PASSWORD_SFA=admin

LTPA_SFA_EXPIRATION=3600

# Library paths - update these for your environment
# Example for Windows: DEV_LIB_DIR=D:\lib\dev
# Example for Linux: DEV_LIB_DIR=/home/user/lib/dev
DEV_LIB_DIR=${user.home}/lib/dev
MYSQL_LIB_DIR=${user.home}/lib/mysql

#POD_NAME=local-server-YOURNAME need to change "YOURNAME" in local
